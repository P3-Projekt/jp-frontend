"use client";

import CanvasComponent, { DisplayMode } from "@/components/map/CanvasComponent";

const Page = () => {
	return <CanvasComponent displayMode={DisplayMode.view} />;
};

export default Page;
